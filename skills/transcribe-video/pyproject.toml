[project]
name = "transcribe-video"
version = "1.0.0"
description = "Extract transcript from local video files using NVIDIA NIM Whisper"
requires-python = ">=3.10"
dependencies = [
    "nvidia-riva-client>=2.0.0",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = []
